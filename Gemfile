# 仅用于本地预览: bundle install && bundle exec jekyll serve
# GitHub Pages 上不需要这个文件(用的是 github-pages 的托管构建)
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-remote-theme"
gem "webrick"
